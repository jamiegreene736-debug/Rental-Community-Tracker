node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
tests/*.debug.png
test-artifacts/

# Never commit real secrets. .env holds Railway/local API keys; only the
# example templates are tracked.
.env
.env.*
!.env.example
!.env.remote-sidecar.example
